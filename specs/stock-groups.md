# VibeSpec：stock-groups

## 1. Spec

### 目标

- 在 VS Code 左侧 `leekFundView.stock` 股票树列表中补充显示股票代码。
- 在原生 TreeView 能力范围内，让每只股票呈现“名称 + 股票代码”和“行情摘要”两层信息表达。
- 新增股票分组功能，用户可以创建分组，并通过拖拽调整分组顺序。
- 支持在分组内展示股票，分组节点可展开和折叠。

### 修改范围

- 股票列表入口限定为当前项目已有的 VS Code 左侧 `leekFundView.stock` TreeView。
- 保留现有市场一级分类：A 股、港股、美股、接口不支持股票。
- 自定义分组只作用于 A 股、港股、美股三个市场分类下，不作用于接口不支持股票。
- 新增已注册配置项 `leek-fund.stockGroups`，用于保存用户创建的分组及分组内股票顺序。
- `leek-fund.stocks` 继续作为股票全集和未分组股票顺序来源。
- 用户通过股票树标题或市场分类右键命令创建分组。
- 用户可拖动分组调整同一市场分类内的分组顺序。
- 用户可拖动股票进入分组、在分组内调整顺序，或拖回市场分类/未分组节点。
- 股票条目补充股票代码，并保留现有涨跌幅、现价等行情摘要展示。

### 非目标范围

- 不把股票列表改造为 Webview。
- 不新增独立股票管理页面。
- 不新增分组重命名、删除、批量管理能力。
- 不新增股票数据源，不修改行情请求接口。
- 不修改股票详情、策略中心、状态栏轮播、价格提醒、新闻或其他非股票树功能。
- 不修改数据库结构、权限认证、登录鉴权、加密逻辑、CI/CD、构建部署脚本、环境变量或生产配置。
- 不新增依赖，不修改 lock 文件，不做无关重构、目录迁移或大面积格式化。
- 不做未说明的兼容分支、兜底或降级逻辑。

### 验收标准

1. 股票树中的股票条目能直接看到股票名称、股票代码、涨跌幅和当前价格。
2. 创建分组命令可在股票树标题或市场分类右键入口触发。
3. 用户创建分组时必须选择或继承所属市场分类，分组名称不能为空，且同一市场分类下分组名不能重复。
4. 自定义分组展示在所属市场分类下，分组节点可展开和折叠。
5. 用户可在同一市场分类内拖动分组调整分组顺序。
6. 用户可拖动同一市场分类内的股票进入分组、在分组内调整顺序，或移动到未分组区域。
7. 禁止跨市场分类拖动股票或分组；发生跨市场拖动时给出提示且不修改配置。
8. 删除股票后，该股票不再残留在任何分组配置中。
9. 未修改非目标范围内的功能，未新增依赖，自动化验证至少通过 TypeScript 编译或明确记录失败原因。

---

## 2. Task Card

### 当前任务

实现原生股票树内的股票代码展示与自定义股票分组。

### 允许修改

- `specs/stock-groups.md`
- `package.json`
- `src/shared/typed.ts`
- `src/shared/leekConfig.ts`
- `src/shared/leekTreeItem.ts`
- `src/explorer/stockProvider.ts`
- `src/registerCommand.ts`

### 禁止修改

- 禁止修改 lock 文件。
- 禁止新增依赖。
- 禁止修改股票详情、策略中心、状态栏轮播、价格提醒、新闻或其他非股票树功能。
- 禁止修改行情数据源和请求接口。
- 禁止自动 commit、push、merge。
- 禁止将任何密钥、Token、生产地址或敏感配置写入仓库。

### 执行步骤

1. 读取本 Spec，确认目标、边界和允许修改文件。
2. 定义股票分组配置类型和配置读写方法，确保配置项已在 `package.json` 中注册。
3. 调整股票树 Provider：市场分类下按自定义分组和未分组展示股票。
4. 实现分组创建命令，支持从市场分类右键继承市场，或从标题入口选择市场。
5. 扩展拖拽逻辑，支持同市场内分组排序、股票入组、组内排序和移出到未分组。
6. 调整股票 TreeItem 文案，补充股票代码并保留行情摘要。
7. 执行 TypeScript 编译验证，不通过时定位并修复本次改动引入的问题。
8. 基于 Spec、Task Card 和完整 `git diff` 填写 Review Report，输出 Diff Summary。

### Review 检查点

- 是否满足股票代码展示和分组功能目标。
- 是否仍限定在原生 `leekFundView.stock` TreeView。
- 是否只在同一市场分类内允许股票和分组拖动。
- `leek-fund.stocks` 与 `leek-fund.stockGroups` 的职责是否清晰。
- 删除股票后分组配置是否同步清理。
- 是否修改了非目标范围内的功能。
- 是否新增依赖或修改 lock 文件。
- `package.json` 配置和命令变更是否已作为高风险修改说明。
- 编译验证是否完成并记录。

### 高风险修改说明

- 修改项：`package.json` 新增命令、菜单入口和配置项 `leek-fund.stockGroups`。
- 必要性：VS Code 命令和设置必须在扩展清单中声明，否则用户无法从股票树入口创建分组，分组配置也不能通过标准设置体系持久化。
- 影响范围：股票树标题菜单、股票树市场分类右键菜单、用户设置导入导出时的 `leek-fund.stockGroups`。
- 验证命令：`npm run compile`。
- 需人工确认：是。合并前需人工确认新增配置项和菜单入口符合插件发布要求。
- 回滚方式：删除 `package.json` 中新增命令、菜单和配置项，删除本 Spec 允许文件中与 `stockGroups` 相关的代码。

---

## 3. Review Report

### Spec 符合性

结论：满足本 Spec 的功能目标，需人工确认后合并。

说明：已在原生 `leekFundView.stock` TreeView 中补充股票代码展示，股票条目主文案显示“名称 + 股票代码”，描述继续展示涨跌幅和现价。已新增 `leek-fund.stockGroups` 配置、创建分组命令、市场分类下的自定义分组/未分组节点、分组折叠，以及同市场内的分组排序、股票入组、组内排序、移出未分组拖拽逻辑。跨市场股票或分组拖动会提示并拒绝写配置。

### Task 完成情况

结论：已完成。

说明：完成分组配置类型、配置读写、股票删除时清理分组、股票置顶时同步组内顺序、TreeView 分组渲染、拖拽 payload 区分、创建分组命令和菜单入口。未新增依赖，未修改 lock 文件，未修改股票详情、策略中心、状态栏、价格提醒或行情数据源。

### Diff 范围检查

结论：符合 Task Card，存在需人工确认的高风险清单变更。

说明：实际修改文件为 `package.json`、`src/shared/typed.ts`、`src/shared/leekConfig.ts`、`src/shared/leekTreeItem.ts`、`src/explorer/stockProvider.ts`、`src/registerCommand.ts` 和本 Spec。`package.json` 新增命令、菜单入口和配置项，属于项目规则列出的高风险修改，合并前需人工确认。

### 风险检查

风险等级：中。

风险项：

- 原生 VS Code TreeView 不支持自定义 HTML 多行卡片，本次按 TreeView 能力实现为“主标签：名称 + 股票代码；描述：涨跌幅 + 现价；tooltip 展示更完整行情”。视觉上不是 Webview 那种严格两行卡片。
- 分组折叠依赖 VS Code TreeView 原生展开/折叠能力；刷新后是否完全保持用户当前折叠状态需在真实 VS Code 扩展窗口中确认。
- 拖拽交互依赖 VS Code TreeView DnD 行为，编译只能验证类型，不能替代真实拖拽验证。
- `package.json` 新增 `leek-fund.stockGroups` 会进入用户设置导入导出范围，需要人工确认配置结构符合发布预期。

### 测试与验证

已执行验证：

- `npm run compile`：通过；`postcompile` 子项目构建通过，保留既有 `import/no-anonymous-default-export` warning。
- `git diff --check`：通过。

未覆盖风险：

- 未在真实 VS Code 扩展窗口中验证创建分组、折叠分组、拖动分组排序、股票入组/移出未分组。
- 未验证已有用户设置中手写异常 `leek-fund.stockGroups` 数据的 UI 表现；代码仅过滤无效分组对象，不做额外修复分支。

### 合并建议

结论：需人工确认。

原因：功能实现和编译验证已完成，但本次必须修改 `package.json` 的命令、菜单和配置声明；按项目规则，合并前需人工确认高风险清单变更及真实 VS Code 交互效果。
