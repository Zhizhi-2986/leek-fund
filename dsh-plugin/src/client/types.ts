/**
 * Client-side type re-exports. Type-only: erased at compile time, so the
 * browser bundle never carries host code through this module.
 */
export type { Quote, Group, State, StockCandidate, Category } from '../types.js'
